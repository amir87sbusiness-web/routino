# 🖥️ راهنمای کامل بک‌اند روتینو

> **بک‌اند یعنی چی؟** سروری که جدا از اپ اجرا می‌شه و کارهایی رو انجام می‌ده که نباید دست کاربر باشه: ورود با پیامک، حساب‌وکتاب اشتراک، پرداخت با درگاه، و پنل مدیریت.
> همه‌ی بک‌اند داخل پوشه‌ی **`backend/`** است و کاملاً مستقل از فرانت اجرا می‌شه (پورت ۳۰۰۰).
>
> اصل طلایی کل بک‌اند: **به کلاینت اعتماد نکن.** قیمت، تخفیف، مدت اشتراک، دوره آزمایشی — همه فقط سمت سرور محاسبه و تایید می‌شن.

---

## ۱. نقشه پوشه‌ی `backend/`

```
backend/
├── src/
│   ├── index.ts          ← نقطه استارت سرور
│   ├── app.ts            ← سرهم‌کردن قطعات (مسیرها، CORS، ...)
│   ├── env.ts            ← 📌 لیست همه تنظیمات محیطی
│   ├── db/               ← دیتابیس سرور (جدول‌ها)
│   ├── routes/           ← آدرس‌های API (هر فایل = یک گروه آدرس)
│   ├── services/         ← منطق اصلی (قیمت، اشتراک، کد پیامکی، توکن)
│   ├── providers/        ← اتصال به سرویس‌های بیرونی (پیامک، درگاه)
│   ├── plugins/          ← احراز هویت + مدیریت خطا
│   └── lib/phone.ts      ← ⚠️ قل دومِ استانداردسازی شماره (باید با فرانت یکی باشه)
├── test/                 ← تست‌های خودکار
├── .pglite-data/         ← 💾 دیتابیس توسعه (فایل‌های دیتا — پاکش کنی همه‌چی صفر می‌شه)
└── package.json          ← دستورها: dev, build, start, test
```

---

## ۲. استارت و تنظیمات

### `backend/src/index.ts` — نقطه شروع

تنها جایی که اتصال واقعی دیتابیس ساخته می‌شه. با توجه به env تصمیم می‌گیره:

- **دیتابیس:** `DB_DRIVER=pglite` (پیش‌فرض توسعه — پستگرس واقعی ولی داخل خود پروسه، بدون نصب هیچی، دیتا در `.pglite-data/`) یا `postgres` (پروداکشن)
- **پیامک:** `SMS_PROVIDER=console` (چاپ کد در ترمینال) یا `kavenegar` (ارسال واقعی)
- **درگاه:** `PSP_PROVIDER=fake` فقط برای توسعه/تست یا `PSP_PROVIDER=zarinpal` برای production. در production فقط زرین‌پال مجاز است.

موقع بالا آمدن: جدول‌ها رو می‌سازه (اگه نباشن)، پلن‌ها رو seed می‌کنه، و هر ۱ ساعت کدهای پیامکی قدیمی‌تر از ۲۴ ساعت رو پاک می‌کنه.

### `backend/src/env.ts` — 📌 همه‌ی تنظیمات محیطی در یک نگاه

| متغیر                | پیش‌فرض                                                  | یعنی                                                                                                                                                                                    |
| -------------------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PORT`               | `3000`                                                   | پورت سرور                                                                                                                                                                               |
| `DB_DRIVER`          | `pglite`                                                 | نوع دیتابیس (پروداکشن: اجباراً `postgres`)                                                                                                                                              |
| `DATABASE_URL`       | `postgres://routino:routino@localhost:5432/routino`      | آدرس پستگرس واقعی                                                                                                                                                                       |
| `CORS_ORIGINS`       | لوکال‌ها + `https://localhost` + `capacitor://localhost` | ⚠️ کدوم آدرس‌ها اجازه دارن به API وصل شن — دو تای آخر برای اپ موبایل **حیاتی**اند                                                                                                       |
| `TRUST_PROXY`        | `false`                                                  | فقط وقتی پشت پروکسی (Caddy/Nginx) هستی `true` کن — وگرنه هرکسی می‌تونه IP جعل کنه و از سقف پیامک رد شه                                                                                  |
| `ADMIN_TOKEN`        | `dev-only-admin-token`                                   | 🔑 رمز پنل ادمین (حداقل ۱۲ کاراکتر)                                                                                                                                                     |
| `JWT_SECRET`         | dev-only...                                              | کلید امضای توکن ورود (حداقل ۳۲ کاراکتر)                                                                                                                                                 |
| `ACCESS_TTL_SECONDS` | `2592000`                                                | عمر توکن امضاشده (۳۰ روز؛ کلاینت زمان واقعی `exp` را از JWT می‌خواند)                                                                                                                   |
| `OTP_PEPPER`         | dev-only...                                              | نمک مخفی هش کدهای پیامکی                                                                                                                                                                |
| `OTP_TTL_SECONDS`    | `120`                                                    | اعتبار کد پیامکی (۲ دقیقه)                                                                                                                                                              |
| `OTP_MAX_ATTEMPTS`   | `3`                                                      | حداکثر حدس اشتباه برای هر کد                                                                                                                                                            |
| `SMS_PROVIDER`       | `console`                                                | `console` یا `kavenegar`                                                                                                                                                                |
| `KAVENEGAR_API_KEY`  | —                                                        | کلید API کاوه‌نگار                                                                                                                                                                      |
| `KAVENEGAR_TEMPLATE` | `routino-otp`                                            | اسم قالب تاییدشده در پنل کاوه‌نگار                                                                                                                                                      |
| `PSP_PROVIDER`       | `fake`                                                   | `fake` فقط توسعه/تست؛ `zarinpal` تنها مقدار مجاز production                                                                                                                             |
| `ZARINPAL_MERCHANT`  | `dev-only-zarinpal-merchant`                             | مرچنت زرین‌پال (۳۶ نویسه؛ فقط وقتی زرین‌پال فعاله). مقدار پیش‌فرض `dev-only-...` مثل `ADMIN_TOKEN`/`JWT_SECRET` عمداً با محافظ production قاطی می‌شه — باید حتماً قبل لایو رفتن عوض بشه |
| `PUBLIC_API_URL`     | `http://localhost:3000`                                  | آدرس عمومی همین سرور (درگاه، مرورگرِ کاربر رو به اینجا برمی‌گردونه — از گوشی باید قابل دسترس باشه)                                                                                      |
| `PUBLIC_WEB_URL`     | `http://localhost:5180`                                  | آدرس نسخه وب اپ (برای برگشت بعد از پرداخت وب)                                                                                                                                           |
| `APP_DEEP_LINK`      | `routino://pay/result`                                   | لینک برگشت به اپ موبایل بعد از پرداخت                                                                                                                                                   |
| `IMPORT_MAX_DAYS`    | `400`                                                    | سقف روزهای قابل قبول موقع انتقال اشتراک قدیمی                                                                                                                                           |

**محافظ‌های پروداکشن (خودکار):** اگه `NODE_ENV=production` باشه و secretها `dev-only` مونده باشن، یا `PSP_PROVIDER=fake` باشه، یا `DB_DRIVER=pglite` باشه → سرور **اصلاً بالا نمیاد**. این عمدیه که با تنظیمات تستی نری روی هوا.

در production هیچ override برای `fake` یا `SMS_PROVIDER=console` وجود ندارد؛ هر دو باعث توقف امن سرویس می‌شوند. مرچنت زرین‌پال هم باید UUID معتبر ۳۶نویسه‌ای باشد.

هر بار که سرور بالا میاد، اگه چیزی تستی باشه با `[!] TEST MODE — …` توی لاگ چاپ می‌شه.

### `backend/src/app.ts` — سرهم‌کننده

Fastify رو می‌سازه، CORS و فشرده‌سازی و helmet رو فعال می‌کنه، مدیریت خطا و پلاگین احراز هویت رو ثبت می‌کنه، و همه‌ی گروه‌های مسیر رو با پیشوند `/v1` رجیستر می‌کنه. هیچ اتصال دیتابیسی اینجا ساخته نمی‌شه (تزریق می‌شه) — همینه که تست‌ها بدون داکر اجرا می‌شن.

---

## ۳. دیتابیس سرور — پوشه `backend/src/db/`

### `backend/src/db/schema.ts` — جدول‌ها به زبان ساده

| جدول           | چی توشه                                                                                                                                                                    | نکته مهم                                                                                         |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `users`        | کاربرها: شماره استاندارد، نام کاربری، هش رمز و cursorهای sync                                                                                                              | شماره و نام کاربری unique                                                                        |
| `records`      | لاگ عمومی sync برای دادهٔ شخصی                                                                                                                                             | کلید `(user_id, kind, id)`، cursor و tombstone را نگه می‌دارد                                    |
| `otp_codes`    | کدهای پیامکی (هش‌شده) + شمارنده تلاش                                                                                                                                       | همین جدول دفترِ سقف ارسال هم هست                                                                 |
| `plans`        | 💰 پلن‌های فروش: `m1`/`m3`/`m12` با قیمت تومان                                                                                                                             | قیمت واقعی اپ اینجاست — تغییر قیمت = آپدیت این جدول                                              |
| `discounts`    | کدهای تخفیف: درصد، سقف استفاده، انقضا، محدود به یک شماره                                                                                                                   |                                                                                                  |
| `redemptions`  | کی از چه کدی استفاده کرده                                                                                                                                                  | هر کاربر هر کد فقط ۱ بار (کلید ترکیبی)                                                           |
| `payments`     | 💳 هر تراکنش: مبلغ trusted، `attempt_id`، `authority` زرین‌پال، وضعیت (`requesting/redirected/verifying/provider_unknown/paid/failed/canceled`)، پلتفرم و **`applied_at`** | `(user_id, attempt_id)` و `authority` unique هستند                                               |
| `grants`       | 📒 **دفترکل اشتراک** — هر تمدید یک سطر: از کجا (تریال/پرداخت/انتقال/ادمین)، چند ماه/روز، انقضای قبل و بعد                                                                  | `payment_id` برای grantهای پرداخت partial-unique است؛ یک payment نمی‌تواند دو بار زمان اضافه کند |
| `entitlements` | جواب فعلی: «این کاربر تا فلان تاریخ دسترسی داره»                                                                                                                           | فقط `services/entitlement.ts` حق نوشتن توش داره                                                  |
| `feedback`     | نظرسنجی‌های کاربرها                                                                                                                                                        |                                                                                                  |
| `admins`       | (رزرو آینده)                                                                                                                                                               |                                                                                                  |

### دو فایل دیگر

| آدرس                       | نقش                                                                                                                                                                                                                         |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `backend/src/db/ddl.ts`    | SQL ساخت جدول‌ها + **`SEED_PLANS_SQL`** (قیمت اولیه: یک‌ماهه ۵۹٬۰۰۰ / سه‌ماهه ۱۴۹٬۰۰۰ / یک‌ساله ۴۴۹٬۰۰۰ تومان — فقط برای نصب اول؛ بعدش قیمت از خود جدول خونده می‌شه) + کد تخفیف تستی `ROUTINO20` (۲۰٪ — به پروداکشن نمی‌ره) |
| `backend/src/db/client.ts` | تایپ مشترک دیتابیس (پستگرس واقعی و PGlite) + تابع `rowsOf` برای SQL خام — دست نزن                                                                                                                                           |

---

## ۴. مسیرهای API — پوشه `backend/src/routes/`

### `backend/src/routes/auth.ts` — ورود با پیامک

| متد و آدرس                     | ورودی                                  | چیکار می‌کنه                                                                                                                                                                         |
| ------------------------------ | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `POST /v1/auth/otp/request`    | `{phone}`                              | چک سقف ارسال ← ساخت کد ۴ رقمی ← ارسال پیامک. جوابش برای شماره‌ی موجود و ناموجود یکسانه (که نشه فهمید کی عضو روتینوئه)                                                                |
| `POST /v1/auth/otp/verify`     | `{phone, code, intent?, newPassword?}` | چک کد ← برای `signup` رمز حساب تازه را ثبت می‌کند؛ برای `password_reset` رمز را عوض می‌کند؛ سپس یک access token سی‌روزه و وضعیت اشتراک را می‌دهد. حساب تازه entitlement=`none` دارد. |
| `POST /v1/auth/password/login` | `{identifier, password}`               | ورود با شماره یا نام کاربری؛ یک access token سی‌روزه می‌دهد و هیچ device/refresh row نمی‌سازد.                                                                                       |

### `backend/src/routes/payments.ts` — 💳 مسیر پول (⚠️ حساس‌ترین فایل بک‌اند)

⚠️ خود `routes/payments.ts` فقط آداپتور نازک Fastify‌ئه (پارس ورودی، رندر پاسخ). **کل منطق پول واقعاً تو `services/payment-flow.ts` زندگی می‌کنه** (بدون وابستگی به فریم‌ورک، عیناً با نسخه‌ی Supabase Edge مشترکه) و صفحه‌ی HTML نتیجه در `lib/pay-result-page.ts` (تابع `renderResultPage`) است. اگه دنبال تغییر رفتار پرداخت اومدی، باید بری سراغ `payment-flow.ts` نه این فایل.

| متد و آدرس                   | چیکار می‌کنه                                                                                                                                                                                                                                  | تابع اصلی در `payment-flow.ts`    |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| `POST /v1/payments/quote`    | پیش‌فاکتور: قیمت پایه + بررسی کد تخفیف. کد نامعتبر = «دلیل» برمی‌گرده نه خطا (که UI پیام درست نشون بده)                                                                                                                                       | (مستقیم از `services/pricing.ts`) |
| `POST /v1/payments/checkout` | ورودی `attemptId` UUID می‌خواهد؛ retry همان خرید همان payment/redirect را می‌گیرد و تماس PSP دوباره ساخته نمی‌شود. مبلغ/ماه/پلن نهایی **سمت سرور** حساب و قبل از I/O ذخیره می‌شوند. سقف ۱۰ خرید در ساعت؛ تخفیف ۱۰۰٪ بدون درگاه                | `checkoutPayment`                 |
| `GET /v1/payments/callback`  | 🌐 فقط `paymentId` + `Authority` + `Status` اسکالر را می‌پذیرد؛ callback مدرک پرداخت نیست. تأیید واقعی فقط Verify سرور-به‌سرور با مبلغ ریالی ذخیره‌شده است. اگر ذخیره Authority قطع شده باشد، فقط Verify موفق اجازه bind و grant اتمیک می‌دهد | `handlePaymentCallback`           |
| `GET /v1/payments/:id`       | استعلام وضعیت برای اپ. **خود-درمانی:** اگه پرداختی در حالت `redirected` گیر کرده (callback هیچ‌وقت نرسیده)، همین‌جا دوباره از درگاه استعلام و در صورت موفقیت فعال می‌شه                                                                       | `pollPayment`                     |

**اعطای اتمیک (`applyPaid`):** درج unique در `grants.payment_id`، افزایش entitlement، audit grant و `payments.applied_at` در یک transaction‌اند. Verify lease دیتابیسی دارد؛ timeout، پاسخ malformed و وضعیت نامشخص terminal نمی‌شوند. کدهای ۱۰۰ و ۱۰۱ زرین‌پال هر دو موفق‌اند و callback/poll هم‌زمان فقط یک grant می‌سازند.

### `backend/src/routes/subscriptions.ts` — اشتراک

| متد و آدرس                           | چیکار می‌کنه                                                                                                                                                                                                                       |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET /v1/subscriptions/me`           | استعلام مستقل «تا کِی دسترسی دارم؟» برای مسیرهای خاص؛ boot معمولی همان وضعیت را داخل `sync/exchange` می‌گیرد                                                                                                                       |
| `POST /v1/subscriptions/trial/start` | فعال‌سازی صریح و فقط یک‌بارهٔ تریال ۷روزه؛ با قفل ردیف کاربر هم ledger و هم entitlement فعلی را چک می‌کند، پس دو دستگاه نمی‌توانند تریال را جمع بزنند                                                                              |
| `POST /v1/subscriptions/import`      | انتقال یک‌باره‌ی اشتراک قدیمیِ محلی به سرور. چون ادعای کلاینته قابل اعتماد نیست، **محدود** می‌شه: فقط یک بار در عمر حساب، حداکثر `IMPORT_MAX_DAYS` (۴۰۰ روز)، فقط بالا بردن انقضا (نه جمع‌زدن)، و ادعای خام برای حسابرسی ثبت می‌شه |
| `GET /v1/subscriptions/grants`       | کل دفترکل تمدیدهای این کاربر (برای پشتیبانی)                                                                                                                                                                                       |

### بقیه‌ی مسیرها

| آدرس فایل                           | آدرس‌های API                                                                                                                      | نقش                                                                                                                                                                          |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `backend/src/routes/plans.ts`       | `GET /v1/plans`                                                                                                                   | عمومی — لیست پلن‌های فعال با قیمت (فرانت صفحه اشتراک از این می‌خونه)                                                                                                         |
| `backend/src/routes/admin.ts`       | `GET /v1/admin/overview`، `GET /v1/admin/users...`، `POST .../grant`، `GET /v1/admin/payments`، `GET/POST /v1/admin/discounts...` | API ادمین: آمار کلی، جستجو و جزئیات کاربر، پرداخت‌ها/گرنت‌ها، هدیه اشتراک و کدهای تخفیف. دستگاه و مسدودسازی وجود ندارد. احراز: هدر `x-admin-token`                           |
| `backend/src/routes/admin-panel.ts` | `GET /admin`                                                                                                                      | 🖥️ **پنل ادمین** — فقط یه مسیر کوتاهه که HTML رو از `lib/admin-page.ts` (ثابت `ADMIN_PAGE`) برمی‌گردونه. ظاهر، متن‌ها و تب‌های پنل همه تو `lib/admin-page.ts` هستن، نه اینجا |
| `backend/src/routes/dev-gateway.ts` | `GET /v1/dev/gateway`                                                                                                             | درگاه تقلبی تست: صفحه «پرداخت موفق / انصراف». فقط وقتی `PSP_PROVIDER=fake` فعاله                                                                                             |
| `backend/src/routes/health.ts`      | `GET /health`، `GET /health/ready`                                                                                                | چک سلامت (دومی دیتابیس رو هم تست می‌کنه — مناسب مانیتورینگ)                                                                                                                  |

---

## ۵. منطق اصلی — پوشه `backend/src/services/`

### `backend/src/services/entitlement.ts` — حساب‌وکتاب اشتراک

- `readEntitlement` — جواب فعلی: `active` / `expired` / `none` + تاریخ انقضا
- `grantInterval` — تمدید با **ماه تقویمی واقعی** (پستگرس حساب می‌کنه؛ «یک سال» = ۱۲ ماه واقعی نه ۳۶۰ روز). اگه اشتراک هنوز فعاله، از انتهای همون ادامه می‌ده (زودتر خریدن = روز نمی‌سوزه). هر تمدید یک سطر در دفترکل `grants` ثبت می‌کنه
- `startTrialOnce` — داخل transaction ردیف user را قفل می‌کند؛ وجود هر grant قبلی یا حتی entitlement بدون ledger را رد می‌کند و فقط برای حساب کاملاً بدون سابقه، دقیقاً ۷ روز `trial` می‌دهد
- `ensureExpiresAt` — مخصوص انتقال اشتراک قدیمی: فقط «بالا می‌بره تا»، هیچ‌وقت جمع نمی‌زنه
- `hasSettledGrant` — «این حساب قبلاً پرداخت یا انتقال داشته؟» (قفل تکرار import)

### `backend/src/services/pricing.ts` — قیمت

- `quote` — قیمت نهایی: پایه ← اِعمال آفر ← اِعمال تخفیف ← گرد کردن. تبدیل تومان→ریال (×۱۰) **فقط همین‌جاست**
- `checkDiscount` — همه دلایل رد کد: ناشناخته/غیرفعال/منقضی/ظرفیت‌تمام/مال یکی دیگه/قبلاً استفاده‌شده
- `redeemDiscount` — مصرف کد؛ فقط **بعد از** موفقیت پرداخت صدا زده می‌شه (خرید نصفه‌کاره کد رو نمی‌سوزونه)

### `backend/src/services/otp.ts` — کدهای پیامکی

- کد: ۴ رقمی از مولد امن؛ فقط هَشِ نمک‌دارش ذخیره می‌شه؛ ۲ دقیقه اعتبار؛ ۳ حدس؛ فقط جدیدترین کد معتبره (درخواست کد جدید = ابطال قبلی)
- 💸 **سقف‌های ارسال (`LIMITS`)** — هر پیامک پول واقعیه:

| سقف               | مقدار                                                           |
| ----------------- | --------------------------------------------------------------- |
| هر شماره در دقیقه | ۱                                                               |
| هر شماره در ساعت  | ۵                                                               |
| هر شماره در روز   | ۱۰                                                              |
| هر IP در ساعت     | ۲۰                                                              |
| کل سیستم در روز   | ۲۰۰۰ (کلید قطع اضطراری — اگه خورد یعنی یه اتفاق غیرعادی افتاده) |

### `backend/src/services/tokens.ts` — توکن‌ها

- فقط یک JWT امضاشده با `sub=userId` و انقضای پیش‌فرض دقیقاً ۳۰ روز صادر می‌شود.
- refresh token، جدول دستگاه، لیست دستگاه، محدودیت نصب و مسدودسازی کاربر وجود ندارد. middleware فقط امضا/انقضا/subject را بررسی می‌کند و برای auth هیچ query دیتابیس نمی‌زند.
- خروج در فرانت local-only است. تغییر رمز توکن‌های قبلی را پیش از انقضا باطل نمی‌کند؛ این trade-off آگاهانه برای معماری ساده و کم‌درخواست است.

### `backend/src/services/sync.ts` — sync فعالِ دادهٔ شخصی

- مسیر اصلی `POST /v1/sync/exchange` در یک invocation اول dirty rowها را push و سپس از cursor قبلی pull می‌کند؛ مسیرهای قدیمی push/pull فعلاً برای تست/سازگاری باقی‌اند. اشتراک هرگز شرط sync نیست.
- kindهای کلادی پروتکل ۲ دقیقاً `categories`، `habits`، `habitMonths`، `tasks`، `timerSessions` و `journal` هستند؛ `logs` فقط مدل روزانهٔ IndexedDB است و تنظیمات همگی local-only هستند. exchange بدون `protocolVersion: 2` رد می‌شود.
- هر `habitMonths` حداکثر ۳۱ سلول روزِ تخت و bounded دارد. push می‌تواند فقط روزهای dirty را بفرستد؛ SQL همان سلول‌ها را با timestamp مستقل داخل ماه کامل merge می‌کند. بنابراین روز جدید با envelope قدیمی‌تر هم گم نمی‌شود، replay مساوی seq ردیف را عوض نمی‌کند و حذف عادت به‌جای صدها روز فقط ماه‌های همان عادت را tombstone می‌کند.
- `sync-record-validation.ts` شکل دقیق هر kind، کلید طبیعی، enumها، عددهای finite، طول رشته و حجم واقعی UTF-8 را قبل از نوشتن بررسی می‌کند. ژورنال حداکثر ۴۰۰۰ کاراکتر و ۱۶ KiB متن UTF-8 دارد؛ packet ماه سقف ۴۴ KiB و بقیهٔ رکوردهای زنده سقف ۲۰ KiB دارند. `data: unknown` دیگر به معنی JSON دلخواه نیست.
- رد semantic به‌صورت `rejectedRecords` و بدون بازتاب متن/دادهٔ خصوصی برمی‌گردد؛ رکوردهای سالم همان batch همچنان ذخیره و pull می‌شوند. فقط envelope خراب یا بیش از ۲۰۰ رکورد، خطای کل درخواست است.
- برای هر حساب دو شمارندهٔ دقیق DB داریم: حداکثر ۵۰٬۰۰۰ ردیف کلادی (tombstone هم حساب می‌شود) و ۱۲۸ MiB متن JSON. triggerهای statement-level در همان تراکنش و بدون query شبکه‌ای اضافه، delta هر batch را روی `users` ثبت می‌کنند؛ check constraint حتی نوشتن مستقیم خارج از API را هم متوقف می‌کند. بک‌فیل فقط بار اول/در migration انجام می‌شود و بوت‌های بعدی اسکن حساب ندارند.
- اگر batch سالم از سقف حساب عبور کند، کل write و افزایش `seq` rollback می‌شود، pull همچنان ادامه دارد و فقط metadata محدود با کد `account_quota_exceeded` برمی‌گردد. کلاینت منبع محلی را dirty نگه می‌دارد؛ متن ژورنال یا payload خصوصی هرگز echo نمی‌شود.
- client با push-before-pull، LWW بر اساس `updatedAt`، clamp ساعت، tombstone، pagination، GC reset و تسویهٔ مستقل هر رکورد کار می‌کند.
- صفحهٔ نهایی pull entitlement را هم برمی‌گرداند تا کاربرِ منقضی هم بتواند تاریخچه‌اش را بازیابی کند.
- Fastify و Hono/Edge هر دو JSON را پیش از parse روی ۶۴ KiB می‌بندند؛ Hono علاوه بر `Content-Length`، stream واقعی را هم بایت‌به‌بایت محدود می‌کند تا هدر حذف‌شده یا دروغین راه دورزدن نباشد.

### `backend/src/services/payment-flow.ts` — 💳 ماشین حالت پرداخت (منطق واقعی مسیر پول)

- `checkoutPayment` / `handlePaymentCallback` / `pollPayment` / `applyPaid` — همون‌هایی که بالاتر تو جدول `routes/payments.ts` بهشون اشاره شد. عیناً (بدون تغییر) با `supabase/functions/api/shared/` سینک می‌شه؛ تست parity جلوی جدایی رو می‌گیره.

### `backend/src/services/admin.ts` — منطق API های پنل ادمین

- `adminOverview`, `adminListUsers`, `adminUserDetail`, `adminGrant`, `adminListPayments`, `adminListDiscounts`, `adminCreateDiscount`, `adminUpdateDiscount` — همه‌ی چیزی که `routes/admin.ts` صداشون می‌زنه

---

## ۶. سرویس‌های بیرونی — پوشه `backend/src/providers/`

| آدرس                         | نقش                                                                    | نکته                                                                                               |
| ---------------------------- | ---------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `providers/sms/console.ts`   | چاپ کد در ترمینال سرور (توسعه)                                         | خط `[sms:console] OTP for 989... -> 123456`                                                        |
| `providers/sms/kavenegar.ts` | ارسال واقعی با کاوه‌نگار (مسیر `verify/lookup`)                        | ⚠️ قالب پیامک باید قبلاً در پنل کاوه‌نگار **تایید** شده باشه (چند روز طول می‌کشه — زودتر اقدام کن) |
| `providers/psp/index.ts`     | قرارداد typed برای create/verify؛ پاسخ خام PSP از این مرز عبور نمی‌کند | production دقیقاً یک adapter می‌سازد                                                               |
| `providers/psp/zarinpal.ts`  | زرین‌پال REST v4 با Rial/IRR، timeout، parsing امن و نتیجه‌های typed   | فقط کد ۱۰۰/۱۰۱ اجازه grant می‌دهد                                                                  |
| `providers/psp/fake.ts`      | fake زرین‌پال‌شکل برای تست خودکار و توسعه                              | production بدون override آن را رد می‌کند                                                           |

---

## ۷. احراز هویت و خطاها — پوشه `backend/src/plugins/`

| آدرس                | نقش                                                                                                                                 |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `plugins/auth.ts`   | نگهبان stateless مسیرهای خصوصی: فقط امضا، انقضا و subject هدر `Authorization: Bearer ...` را چک می‌کند؛ query دیتابیس ندارد         |
| `plugins/errors.ts` | فرمت استاندارد همه خطاها: `{error: "کد", message: "متن"}` + کد HTTP. خطاهای پیش‌بینی‌نشده = ۵۰۰ بدون افشای جزئیات (فقط در لاگ سرور) |

**کدهای خطای مهم که فرانت می‌شناسه:** `invalid_phone`، `rate_limited` (+هدر Retry-After)، `sms_failed`، `bad_code`، `psp_failed`، `unknown_plan`، `not_signed_in` — لیست ترجمه‌شون در فایل [03-FRONT-BACK-CONNECTIONS.md](03-FRONT-BACK-CONNECTIONS.md).

---

## ۸. تست‌ها — پوشه `backend/test/`

با `cd backend && npm test` اجرا می‌شن. بدون داکر و بدون نصب پستگرس (PGlite تزریق می‌شه):

| فایل                       | چی رو تست می‌کنه                                                                                                                                         |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auth.test.ts`             | ورود، سقف پیامک، تریال                                                                                                                                   |
| `otp-abuse-limits.test.ts` | سقف ۲۰ پیامک در ساعت برای هر IP و قطع‌کنندهٔ سراسری قبل از پیامک ۲۰۰۱م — بدون تماس واقعی با SMS                                                          |
| `payments.test.ts`         | کل ماشین پرداخت (موفق/لغو/جعل/idempotency checkout/دوباره‌گرنت‌نشدن)                                                                                     |
| `pricing.test.ts`          | قیمت و تخفیف‌ها                                                                                                                                          |
| `entitlement.test.ts`      | تمدید و دفترکل                                                                                                                                           |
| `subscriptions.test.ts`    | انتقال اشتراک قدیمی و محدودیت‌هاش                                                                                                                        |
| `admin.test.ts`            | پنل ادمین                                                                                                                                                |
| `admin-page.test.ts`       | صفحهٔ واقعی پنل ادمین: پاسخ overviewِ ورود فقط یک‌بار گرفته و نمایش داده می‌شود                                                                          |
| `phone.parity.test.ts`     | ⚠️ **یکسان‌بودن بایت‌به‌بایت `src/lib/phone.ts` و `backend/src/lib/phone.ts`** + بردارهای رفتاری نرمال‌سازی — اگه یکی رو عوض کنی این تست می‌شکنه (عمداً) |
| `app.test.ts`              | ساخت اپ Fastify، health check ها                                                                                                                         |
| `psp.test.ts`              | create/verify زرین‌پال، کدهای ۱۰۰/۱۰۱، خطاهای رسمی، timeout/network و پاسخ malformed با fetch mock                                                       |
| `edge-parity.test.ts`      | یکسان‌بودن `backend/src` با کپیِ سینک‌شده در `supabase/functions/api/shared/` — اگه فراموش کنی `sync:edge` بزنی این می‌شکنه                              |
| `helpers/`                 | ابزار ساخت اپ تستی                                                                                                                                       |

---

## ۹. کارهای رایج بک‌اند 🍳

| می‌خوای...                   | چیکار کن                                                                                                                                                                                                         |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 💰 قیمت پلن عوض کنی          | جدول `plans` در دیتابیس (پنل ادمین فعلاً قیمت رو ویرایش نمی‌کنه — SQL مستقیم یا ابزار دیتابیس). برای نصب‌های تازه: `SEED_PLANS_SQL` در `db/ddl.ts`. ⚠️ ثابت `PLANS` فرانت (`src/lib/presets.ts`) رو هم هماهنگ کن |
| 🎁 مدت تریال عوض کنی         | `days: 7` داخل `startTrialOnce` در `services/entitlement.ts`                                                                                                                                                     |
| 🏷️ کد تخفیف بسازی/خاموش کنی  | پنل `/admin` تب تخفیف‌ها (یا `POST /v1/admin/discounts`)                                                                                                                                                         |
| 👤 به کاربری اشتراک هدیه بدی | پنل `/admin` ← جستجوی شماره ← دکمه هدیه (یا `POST /v1/admin/users/:id/grant`)                                                                                                                                    |
| 📲 پیامک واقعی               | env: `SMS_PROVIDER=kavenegar` + `KAVENEGAR_API_KEY` + قالب تاییدشده                                                                                                                                              |
| 💳 درگاه واقعی               | env: `PSP_PROVIDER=zarinpal` + `ZARINPAL_MERCHANT` + آدرس‌های عمومی (`PUBLIC_API_URL` و `PUBLIC_WEB_URL`)                                                                                                        |
| 🔢 سقف پیامک عوض کنی         | ثابت `LIMITS` در `services/otp.ts`                                                                                                                                                                               |
| ⏱️ عمر توکن/کد عوض کنی       | env: `ACCESS_TTL_SECONDS` / `OTP_TTL_SECONDS` (اگه access رو عوض کردی فایل ۰۳ بخش توکن رو ببین)                                                                                                                  |
| 🗑️ دیتابیس توسعه رو صفر کنی  | سرور رو خاموش کن و پوشه `backend/.pglite-data/` رو پاک کن                                                                                                                                                        |
| ❓ «پول دادم فعال نشد»       | پنل `/admin` ← کاربر ← جدول Grants و Payments — وضعیت `verify_failed` یعنی اختلاف مبلغ با درگاه (لاگ سرور رو ببین)                                                                                               |

---

## ۱۰. ⚠️ چیزهایی که در بک‌اند دست نزنی مگر با فهم کامل

1. **`services/payment-flow.ts`** (نه `routes/payments.ts`) — منطق `applied_at` و مقایسه مبلغ. جابه‌جایی یک شرط = یا دوباره‌شارژ مفتی یا سوختن پول کاربر.
2. **`lib/phone.ts`** — باید بایت‌به‌بایت با `src/lib/phone.ts` فرانت یکی بمونه (تست parity داره).
3. **`services/entitlement.ts`** — تنها نویسنده‌ی `entitlements`؛ دفترکل `grants` هیچ‌وقت ویرایش نمی‌شه.
4. **ترتیب چک‌ها در `services/otp.ts`** — «تلاش قبل از مقایسه شمرده می‌شه» عمدیه (کرش وسط کار = حدس مفتی نده).
5. **`env.ts` محافظ‌های production** — حذفشون نکن؛ همونان که نمی‌ذارن با تنظیمات تستی لایو بری.
