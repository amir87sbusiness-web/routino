# 🎨 راهنمای کامل فرانت‌اند روتینو

> **فرانت‌اند یعنی چی؟** هر چیزی که کاربر می‌بینه و لمس می‌کنه: صفحه‌ها، دکمه‌ها، رنگ‌ها، انیمیشن‌ها، و دیتابیس محلیِ داخل خود گوشی/مرورگر.
> همه‌ی فرانت داخل پوشه‌ی **`src/`** است + چند فایل تنظیمات در ریشه‌ی پروژه.
>
> این فایل مکمل [CODEBASE_GUIDE.md](CODEBASE_GUIDE.md) است — اونجا نقشه‌ی کلی و «دستور پخت‌ها» رو داری؛ اینجا جزئیات کامل هر فایل فرانت.

---

## ۱. نقشه پوشه‌ی `src/`

```
src/
├── client.tsx            ← روشن‌شدن اپ (نقطه استارت)
├── routeTree.gen.ts      ← ⛔ تولید خودکار — هرگز دستی ویرایش نکن
├── styles.css            ← 🎨 همه رنگ‌ها و تم روشن/تیره
├── routes/               ← 📱 صفحه‌های اپ (هر فایل = یک صفحه)
├── components/           ← 🧩 قطعه‌های تکرارشونده (کارت عادت، فرم، منو...)
├── state/                ← 🧠 مغز اپ (فقط یک فایل: app.tsx)
├── lib/                  ← ⚙️ منطق و محاسبات (بدون ظاهر)
│   ├── db/               ← 💾 ذخیره‌سازی محلی
│   └── api/              ← 🌐 ارتباط با سرور
└── test/                 ← تنظیمات تست‌ها
```

---

## ۲. نقطه شروع اپ — چطور روشن می‌شه؟

| آدرس فایل | نقش | توضیح ساده |
|---|---|---|
| `index.html` | صفحه پایه | تنها فایل HTML واقعی. عنوان تب مرورگر، لود فونت وزیرمتن، و `<div id="root">` که کل اپ داخلش رندر می‌شه |
| `src/client.tsx` | استارتر | ۱) روتر رو می‌سازه ۲) PWA رو راه می‌ندازه (`initPwa`) ۳) روی موبایل به دیپ‌لینک `routino://pay/result` گوش می‌ده (بازگشت از درگاه پرداخت) ۴) اپ رو رندر می‌کنه |
| `src/routes/__root.tsx` | قالب سراسری | دور همه‌ی صفحه‌ها می‌پیچه: `AppProvider` (مغز اپ)، `Toaster` (پیام‌های کوچک پایین صفحه)، `UpdateWatcher` (اعلان نسخه جدید)، صفحه ۴۰۴ و صفحه خطا |
| `src/routeTree.gen.ts` | ⛔ خودکار | با هر بار اجرای dev/build از روی پوشه `routes/` ساخته می‌شه. آدرس صفحه‌ها از اسم فایل‌ها میاد (مثلاً `habits.tsx` → آدرس `/habits`) |

**زنجیره‌ی روشن‌شدن:** `index.html` ← `client.tsx` ← `__root.tsx` ← `AppProvider` (لود دیتا) ← `AppShell` (چک گِیت) ← صفحه‌ی مقصد

---

## ۳. مغز اپ — `src/state/app.tsx` (مهم‌ترین فایل فرانت)

این فایل «AppProvider» رو تعریف می‌کنه که همه‌ی صفحه‌ها ازش تغذیه می‌شن. هر صفحه با `useApp()` یا `useAppMaybe()` به این ۵ چیز دسترسی داره:

| چیز | یعنی |
|---|---|
| `db` | کل دیتای اپ در حافظه (عادت‌ها، کارها، ژورنال، تنظیمات...) — شکلش در `lib/store.ts` تعریف شده |
| `update(fn)` | **تنها راه تغییر دیتا.** یه تابع می‌گیره که db قدیمی رو می‌گیره و db جدید برمی‌گردونه |
| `lang` / `cal` | زبان فعلی (`fa`/`en`) و تقویم فعلی (`jalali`/`gregorian`) |
| `t(fa, en)` | تابع ترجمه: اگه زبان فارسیه متن اول، وگرنه متن دوم |

**کارهایی که این فایل خودکار انجام می‌ده (به ترتیب داخل فایل):**

1. **لود اولیه (hydrate):** موقع باز شدن اپ، دیتا رو از دیسک می‌خونه + شمارنده جلسات (`sessions`) رو یکی زیاد می‌کنه + دستکاری ساعت رو چک می‌کنه.
2. **ذخیره خودکار:** بعد از هر `update`، تغییرها رو تشخیص می‌ده و در پس‌زمینه ذخیره می‌کنه — UI هیچ‌وقت منتظر ذخیره نمی‌مونه (به همین دلیل آفلاین و آنلاین فرقی حس نمی‌شه).
3. **تأیید نشست و اشتراک:** روی boot/برگشت اینترنت/foreground و هر ۶۰ ثانیهٔ foreground دستگاه را تأیید می‌کند؛ جواب موفق lease آفلاین ۱۵روزه را تازه می‌کند و اشتراک را هم refresh می‌کند. بعد از ۱۵ روز آفلاین فقط صفحهٔ اتصال نشان داده می‌شود و دیتا پاک نمی‌شود.
4. **ضربان‌سنج (heartbeat):** هر ۶۰ ثانیه `lastSeen` رو به‌روز می‌کنه. اگه ساعت دستگاه بیش از **۶ ساعت** (`TAMPER_TOLERANCE`) به عقب کشیده بشه → `tampered = true` → اشتراک قفل می‌شه تا تمدید کنه. (قبلاً ۵ دقیقه بود؛ گوشی‌ای که مدتی خاموش یا بی‌آنتن بوده موقع تصحیح خودکار ساعت بیشتر از این جابه‌جا می‌شه و بی‌گناه قفل می‌شد. برای کش‌رفتن اشتراک باید ساعت روزها عقب بره که همچنان گرفته می‌شه.) پاک‌شدن این پرچم فقط با جواب سرور ممکنه — و اون درخواست حالا در صورت شکست هر ۶۰ ثانیه دوباره تلاش می‌کنه.
5. **اعمال تم/زبان/رنگ:** کلاس `dark`، جهت `rtl/ltr`، و متغیر CSS `--primary` (رنگ برند) رو روی صفحه می‌ذاره.
6. **یادآورهای نیتیو:** روی موبایل، آلارم عادت/کار/ژورنال و ۳ روز مانده/روز پایان اشتراک را با سیستم‌عامل زمان‌بندی می‌کند.
7. **یادآور داخل اپ/وب:** هر ۳۰ ثانیه یادآورهای روزانه و روی تغییر اشتراک اعلان پایان را می‌سازد؛ اگر مرورگر اجازه بدهد Notification سیستمی هم نمایش می‌دهد.

---

## ۴. منطق و محاسبات — پوشه `src/lib/`

### `src/lib/store.ts` — «فرهنگ لغت» دیتا
تعریف اینکه هر موجودیت چه فیلدهایی داره. اگه بخوای بدونی «عادت چیا داره؟» جواب اینجاست:

| موجودیت | فیلدهای مهم |
|---|---|
| `Settings` | زبان، تقویم، تم، رنگ برند، `onboarded`، ساعت یادآور ژورنال (پیش‌فرض `22:00`)، روشن‌بودن نوتیف |
| `Habit` (عادت) | اسم، دسته، نوع (`binary`=شد/نشد یا `quantity`=مقداری)، هدف (`target` — برای عادت زمانی = کل دقیقه)، واحد، برنامه هفتگی (`schedule`)، هدف ماهانه، ساعت یادآور، تاریخ ساخت |
| `HabitLog` (ثبت روزانه) | کلیدش `habitId|تاریخ` است؛ مقدار، انجام‌شده، یادداشت، حس‌وحال |
| `Task` (کار) | تاریخ، عنوان، نوع، هدف/مقدار، یادداشت، ساعت یادآور، رنگ، آیکون |
| `TimerSession` | حالت تایمر، ثانیه‌های تمرکز، شروع/پایان، اتصال به عادت/کار |
| `JournalEntry` | متن، نمره ۱–۱۰، حس‌وحال، به کلید تاریخ |
| `Subscription` | پلن، شروع، انقضا (`expiresAt`)، آزمایشی بودن |
| `meta` | شمارنده جلسات، آخرین نظرسنجی، `lastSeen` و `tampered` (ضد دستکاری ساعت)، جشن‌های داده‌شده، یادآورهای شلیک‌شده |

همین‌جا: `uid()` (تولید شناسه تصادفی) و `defaultDb()` (دیتای کاربر تازه‌وارد).

### `src/lib/logic.ts` — ماشین‌حساب اپ
همه‌ی فرمول‌ها، بدون هیچ ظاهری:

| تابع | چیکار می‌کنه |
|---|---|
| `isDueOn(habit, روز)` | «این عادت امروز فعاله؟» (روزانه/زوج/فرد/روزهای خاص هفته + قبل از تاریخ ساخت هیچ‌وقت) |
| `rawPercent` / `cappedPercent` | درصد انجام یک روز (خام می‌تونه از ۱۰۰ رد بشه، سقف‌دار نه) |
| `isCompleted` | انجام‌شده حساب می‌شه؟ (برای مقداری: رسیدن به هدف هم کافیه) |
| `dayScore` | نمره کل روز = میانگین درصد عادت‌های فعال اون روز |
| `streak` 🔥 | چند روزِ فعالِ پشت‌سرهم انجام شده (اگه امروز هنوز نه، از دیروز می‌شمره؛ حداکثر ۷۳۰ روز عقب می‌ره) |
| `monthProgress` | پیشرفت به سمت هدف ماهانه (روزهای انجام‌شده / هدف) |
| `earnedBadges` 🏅 | همه نشان‌ها — از روی خودِ لاگ‌ها حساب می‌شه (نه ذخیره)، پس هیچ‌وقت غلط نمی‌مونه |
| `successRate` | درصد موفقیت N روز اخیر |
| `habitSeries` / `overallSeries` / `weekComparison` | داده‌های خام نمودارها |
| `subscriptionActive` 🔑 | «اشتراک فعاله؟» — اگه `tampered` باشه همیشه «نه» |

### `src/lib/dates.ts` — تقویم و تاریخ
تبدیل شمسی↔میلادی (کتابخانه jalaali-js)، کلید تاریخ (`YYYY-MM-DD` همیشه میلادی)، اسم ماه‌ها و روزها (فارسی/انگلیسی + کوتاه‌شده)، شروع هفته (شنبه برای شمسی، یکشنبه برای میلادی)، عدد فارسی‌کن (`faNum`)، فرمت‌های نمایش تاریخ، و بازه‌های نمودار آنالیز (`analyticsDayKeys`: هفته=۷ روز اخیر، ماه=۳۱، سه‌ماهه=۹۱، سال=۱۲ ماه تقویمی).

### `src/lib/presets.ts` — محتوای آماده
- `DEFAULT_CATEGORIES` — ۱۶ دسته پیش‌فرض (صبحگاهی، سلامتی، ورزش، ... ، محدودیت‌ها) با رنگ و آیکون
- `PRESET_HABITS` — عادت‌های آماده‌ی هر دسته (نام فارسی/انگلیسی، نوع، هدف، واحد)
- `PLANS` — نسخه‌ی آفلاین قیمت پلن‌ها (باید با سرور یکی باشه — فایل ۰۳ رو ببین)
- `CATEGORY_COLOR_CHOICES` — ۲۰ رنگ قابل انتخاب
- `MOOD_EMOJIS` — ۱۰ ایموجی حس‌وحال

### بقیه‌ی `lib/`

| آدرس | نقش |
|---|---|
| `src/lib/chart.ts` | تبدیل سری روزانه به ستون‌های نمودار: هفته/ماه = ستونِ روزانه؛ سه‌ماهه = هر ستون ۷ روز؛ سال = هر ستون یک ماه تقویمی |
| `src/lib/phone.ts` | ⚠️ استانداردکردن شماره موبایل ایرانی به `989xxxxxxxxx` + تبدیل ارقام فارسی/عربی به انگلیسی. **قل دومش در بک‌اند است و باید یکسان بمونن** |
| `src/lib/backup.ts` | Export محتوای قابل‌حمل بدون auth/subscription/device metadata؛ همیشه فعال. `restoreDb` محتوای فعلی را جایگزین ولی هویت/اشتراک/تم/نوتیف دستگاه را حفظ می‌کند. Import با `src/lib/import-policy.ts` فقط برای پلن پولی فعال است (trial/free/expired ممنوع) |
| `src/lib/pwa.ts` | نصب‌پذیری PWA، ثبت سرویس‌ورکر (فقط وب)، درخواست «دیتامو پاک نکن» از مرورگر (`requestPersistentStorage`) |
| `src/lib/native.ts` | هماهنگی رنگ نوار وضعیت اندروید/iOS با تم (روی وب هیچ کاری نمی‌کنه) |
| `src/lib/native-notifications.ts` | آلارم‌های سیستم‌عاملی موبایل: یادآور تکرارشونده عادت‌ها و ژورنال + یادآور تک‌بارِ کارها. وقتی اپ کاملاً بسته‌ست هم کار می‌کنن. متن نوتیف‌های موبایل اینجاست |
| `src/lib/wipe.ts` | جداسازی دیتا موقع تعویض شماره ورود (`loginAs`, `wipeContent`) — از `auth.tsx` صدا زده می‌شه |
| `src/lib/seed-demo.ts` | دکمه‌ی «دیتای نمایشی» در تنظیمات — یک سال دیتای ساختگی برای دمو (`applyDemoContent`) |
| `src/lib/backup-native.ts` | خروجی فایل پشتیبان روی موبایل با share sheet کپسیتور (`shareBackupNative`) — چون دانلود مستقیم مرورگر اونجا کار نمی‌کنه |
| `src/lib/utils.ts` | تابع کمکی `cn` برای ترکیب کلاس‌های Tailwind |
| `src/lib/legal-info.ts` | اطلاعات تماس/هویتیِ متن قانونی (نام مالک، تلفن، ایمیل، نشانی) + تاریخ آخرین به‌روزرسانی. **تنها فایلی که کاربر برای اینماد باید مقدارش را پر کند** — استفاده در `components/LegalContent.tsx` |

---

## ۵. ذخیره‌سازی محلی — پوشه `src/lib/db/` ⚠️ منطقه حساس

دیتا **داخل خود دستگاه** ذخیره می‌شه (IndexedDB مرورگر/WebView). اینترنت لازم نیست.

| آدرس | نقش | توضیح ساده |
|---|---|---|
| `src/lib/db/dexie.ts` | تعریف جدول‌ها | ۸ جدول: categories, habits, logs, tasks, timerSessions, journal, settings, feedback. هر رکورد یه «پاکت» داره: `updatedAt` (زمان تغییر)، `deleted` (سنگ‌قبر حذف)، `dirty` (منتظر ارسال به سرور — برای سینک آینده)، `seq` (حفظ ترتیب نمایش) |
| `src/lib/db/local.ts` | چیزهای مخصوصِ این دستگاه | در localStorage (`routino:local:v1`): ورود، اشتراکِ کش‌شده، اعلان‌ها، meta، تم، نوتیف. دو لیست مهم: `SYNCED_SETTING_KEYS` (تنظیماتی که بین دستگاه‌ها مشترک می‌شن: زبان، تقویم، رنگ برند، onboarded، ساعت ژورنال) و `LOCAL_SETTING_KEYS` (مخصوص دستگاه: تم، نوتیف) |
| `src/lib/db/hydrate.ts` | خوندن موقع استارت | همه جدول‌ها رو می‌خونه و شیء `Db` رو می‌سازه. برای نصب تازه، ۱۶ دسته پیش‌فرض رو می‌کاره (`seedIfEmpty`) |
| `src/lib/db/diff.ts` | تشخیص تغییر | **حساس‌ترین فایل ذخیره‌سازی.** با مقایسه‌ی مرجع (reference) می‌فهمه دقیقاً کدوم رکوردها عوض شدن — چیزی که اینجا جا بیفته، هیچ‌وقت ذخیره نمی‌شه |
| `src/lib/db/persist.ts` | نوشتن روی دیسک | تغییرها رو در یک تراکنش می‌نویسه؛ حذف = رکورد سنگ‌قبر (نه پاک‌شدن کامل). تابع `pendingChanges` = صف ارسال برای سینک آینده |
| `src/lib/db/migrate.ts` | مهاجرت نسخه قدیمی | دیتای فرمت قدیمی (`routino:v1` در localStorage) رو یک بار به IndexedDB منتقل می‌کنه. **نسخه قدیمی عمداً پاک نمی‌شه** (بیمه‌ی بازیابی) |

---

## ۶. ارتباط با سرور — پوشه `src/lib/api/`

(جزئیات کامل قراردادها در فایل [03-FRONT-BACK-CONNECTIONS.md](03-FRONT-BACK-CONNECTIONS.md))

| آدرس | نقش |
|---|---|
| `src/lib/api/client.ts` | موتور ارسال درخواست. آدرس سرور: روی وب `/v1` (پروکسی به لوکال)، روی موبایل از متغیر `VITE_API_URL`. روی موبایل از موتور نیتیو Capacitor استفاده می‌کنه (بدون مشکل CORS). قطعی اینترنت = خطای عادی با پرچم `offline`، نه فاجعه |
| `src/lib/api/auth.ts` | توکن‌های ورود در localStorage (`routino:auth:v1`). تمدید خودکار توکن ۱ دقیقه قبل از انقضا؛ اگه چند درخواست همزمان ۴۰۱ بگیرن فقط یکی تمدید می‌کنه (بقیه منتظر می‌مونن). **قانون طلایی: آفلاین‌بودن هیچ‌وقت باعث خروج از حساب نمی‌شه** |
| `src/lib/api/payments.ts` | چهار تابع: `fetchPlans` (لیست پلن‌ها)، `fetchQuote` (استعلام قیمت+تخفیف)، `checkout` (شروع خرید)، `fetchPayment` (وضعیت پرداخت) |

گزارش فنی محدود در `src/lib/diagnostics.ts` فقط روی همان دستگاه می‌ماند: حداکثر ۱۰۰ رویداد برای ۷ روز. متن کاربر، شماره، نام کاربری، OTP، توکن، بدنه و Query String ثبت یا خودکار ارسال نمی‌شوند. `exportDiagnostics()` خروجی پشتیبانی می‌سازد و `clearDiagnostics()` آن را پاک می‌کند.

---

## ۷. صفحه‌های اپ — پوشه `src/routes/`

> قانون: اسم فایل = آدرس صفحه. `index.tsx`=`/`، `habits.tsx`=`/habits`، `habit.$habitId.tsx`=`/habit/یک-شناسه`، `pay.result.tsx`=`/pay/result`

### `src/routes/index.tsx` — صفحه «امروز»
- سلامِ ساعتی (صبح/ظهر/عصر بخیر — متنش همین‌جاست) + تاریخ امروز
- حلقه‌ی درصد روز (انیمیشن پرشدن از صفر) — از `dayScore` تغذیه می‌شه
- نوار هفته (`WeekStrip`) با تعداد عادت‌های هر روز و حلقه درصد
- کارت «کارها» (از `components/tasks.tsx`) + لیست عادت‌های فعالِ روزِ انتخابی (`HabitRow`)
- پاپ‌آپ جشن وقتی به ۷۰٪ یا ۱۰۰٪ هدف ماهانه برسی

### `src/routes/habits.tsx` — مدیریت عادت‌ها
- دو دکمه بالا: «عادت‌های آماده» (مودال پیشنهادها) و «عادت دلخواه» (فرم خالی)
- نوار فیلتر: همه / انجام‌نشده / انجام‌شده / دسته‌ها (فقط دسته‌هایی که عادت دارن) / + دسته جدید
- کارت هر عادت: پیشرفت ماه + استریک 🔥 + مداد (ویرایش) + سطل (حذف با تاییدیه — لاگ‌هاش هم پاک می‌شن)
- رسیدن به هدف ماهانه = کل کارت سبز می‌شه 🏆
- بخش نشان‌ها (Badges) پایین صفحه
- نکته: اگه دسته‌ی پیش‌فرضی رو حذف کرده باشی و از همون دسته عادت آماده اضافه کنی، دسته خودکار برمی‌گرده (`ensureCategory`)

### `src/routes/habit.$habitId.tsx` — جزئیات یک عادت
سه کارت آمار (استریک/موفقیت/این ماه) + نوار هدف ماهانه + نمودار روند (هفته/ماه/سه‌ماهه/سال) + یادداشت‌های اخیر (۱۰ تای آخر)

### `src/routes/tasks.tsx` — کارها
- نوار هفته + انتخاب روز: امروز / فردا / دلخواه (تقویم بازشو)
- افزودن سریع (فقط متن + Enter) و فرم «پیشرفته»: تاریخ، آیکون، رنگ، یادداشت، یادآور ساعتی
- ثابت‌های پیش‌فرض: `TASK_DEFAULT_COLOR` و `TASK_DEFAULT_ICON = "star"`
- یادآور کار روی موبایل به آلارم سیستم‌عامل هم می‌ره (`scheduleTaskReminder`)

### `src/routes/timer.tsx` — تایمر
- ۳ حالت: پومودورو (پیش‌فرض ۲۵/۵ — لیست `POMODORO_PRESETS`)، تایمر آزاد (`FREE_PRESETS`: ۵ تا ۶۰ دقیقه + مدت دلخواه)، کرونومتر
- **دورها (چرخه) در پومودورو**: کاربر انتخاب می‌کند چند دورِ «تمرکز + استراحت» پشت سر هم اجرا شود (`CYCLE_CHOICES` = ۱،۲،۳،۴،۶،۸،۱۰ — پیش‌فرض ۴). مثلاً ۴ دورِ ۲۵/۵ یا ۱۰ دورِ ۱۵/۳. چیپ «دور ۲ از ۴» بالای تایمر وضعیت را نشان می‌دهد (وقتی تعداد ۱ باشد پنهان است)
  - بعد از **آخرین** تمرکز استراحتی نمی‌آید و کل ست تمام می‌شود؛ استراحتی که دور بعدی ندارد فقط انتظار است
  - دور با عوض‌کردن پریست، تعویض حالت، دکمه‌ی ریست و پایان ست به ۱ برمی‌گردد. کم‌کردن تعداد به عددی کمتر از دورِ فعلی، دور را عقب می‌کشد — وگرنه «دور ۵ از ۳» نشان داده می‌شود و ست هیچ‌وقت تمام نمی‌شود
  - `pomoCycles`/`pomoRound` مثل بقیه‌ی حالت‌های تایمر **ref هم دارند**: تیک داخل یک `setInterval` بسته‌شده اجرا می‌شود و state تازه را نمی‌بیند
- اتصال به عادت/کارِ **زمانی** امروز: دقیقه‌های تمرکز (نه استراحت!) خودکار به هدفش اضافه می‌شه
- تاریخچه جلسات (حداکثر ۲۰۰ تا نگه می‌داره؛ ۶ تای آخر نمایش داده می‌شه)
- نکته فنی: منطق تیک‌خوردن با ref نوشته شده تا StrictMode ریکت باعث دوبار ثبت‌شدن نشه — دست نزن

### `src/routes/journal.tsx` — ژورنال
نوار هفته (با ایموجیِ حسِ هر روز به‌جای عدد) + انتخاب حس (۱۰ ایموجی) + نمره ۱–۱۰ + متن آزاد + دکمه ذخیره + تاریخچه ۱۴ روز اخیر. روزهای آینده قفل‌اند (`disableFuture`).

### `src/routes/analytics.tsx` — آنالیز
- مقایسه این‌هفته/هفته‌قبل + پیام «📈 بهتر از هفته قبل!»
- نمودار عملکرد کلی با ۴ بازه (`RANGES`)
- «عملکرد هر عادت»: شبکه‌ی تقویمی ماهانه برای هر عادت + ورق‌زدن ماه‌ها (فلش یا کشیدن انگشت)
- ستون کارهای انجام‌شده/نشده (هر کدوم تا ۳۰ تا)
- کارها عمداً در نمودارها حساب **نمی‌شن** — فقط عادت‌ها

### `src/routes/settings.tsx` — تنظیمات
- کارت حساب (شماره + تاریخ انقضای اشتراک + دکمه تمدید)
- زبان / تقویم / تم / رنگ برند (ثابت `BRAND_COLORS`: پیش‌فرض + ۷ رنگ + چرخ رنگ دلخواه)
- مدیریت دسته‌ها (بازشو): لیست + حذف + ساخت جدید
- کلید نوتیفیکیشن + ساعت یادآور ژورنال (انتخاب‌گر ساعت) + دکمه‌ی **«نظر بده»** (همان `FeedbackModal`) — چون پاپ‌آپ خودکار حالا حداکثر روزی یک‌بار می‌آید و بدون این دکمه کسی که همان لحظه حرفی دارد راهی ندارد
- کارت Export/Import، سلامت فضای مرورگر و درخواست persistent storage، و فهرست دستگاه‌های حساب. Export همیشه فعال؛ Import فقط پلن پولی.
- خروج از حساب (توکن محلی پاک می‌شه، بعد به سرور خبر می‌ده — آفلاین هم کار می‌کنه)
- بخش بازشوی «قوانین، حریم خصوصی و تماس» (کامپوننت `LegalContent`) — لازمِ اینماد

### `src/routes/onboarding.tsx` — خوش‌آمدگویی
۳ اسلاید معرفی (آرایه `slides` — ایموجی/عنوان/متن هر اسلاید همین‌جاست) + صفحه تنظیمات اولیه (زبان/تقویم/تم) + دکمه «شروع کن!» که `onboarded` رو true می‌کنه و می‌فرسته به `/auth`.

### `src/routes/auth.tsx` — ورود
- **دو روش**: پیش‌فرض **ورود با رمز** (شماره موبایل یا نام کاربری + رمز → `passwordLogin`)؛ لینک «ورود با کد پیامکی» به‌عنوان جایگزین برای بار اول یا فراموشی رمز (همان دو مرحله‌ی قدیمی: شماره ← کد ۶ رقمی)
- تابع `completeLogin()` مشترک بین هر دو روش: انتقال اشتراک قدیمیِ محلی (`importSubscription`) + `loginAs` (جداسازی حساب بر اساس شماره‌ی سرور `res.user.phone`)
- ارقام فارسی کیبورد فارسی درست تبدیل می‌شن (باگ رایج حل شده)
- تابع `explain()`: ترجمه‌ی کدهای خطای سرور (از جمله `bad_credentials`) به پیام فارسی قابل فهم
- تنظیم نام کاربری و رمز از **تنظیمات** انجام می‌شه (کارت «نام کاربری و رمز عبور» → `fetchAccount`/`setUsername`/`setPassword`)
- ⚠️ یک پرچم تستی مونده: `SKIP_SMS` — الان `false`ه و باید `false` بمونه (دکمه‌ی `TEST_LOGIN_BUTTON` حذف شد)
- فرم خالی بالا می‌آید. قبلاً شماره از صفحه‌ی معرفی پیش‌پر می‌شد (`?start=…`)، ولی آن مسیر حذف شد — پایین‌تر، بخش «صفحه‌ی معرفی» را ببین

### `src/routes/subscribe.tsx` — دیوار اشتراک (پی‌وال)
- لیست پلن‌ها از سرور (`fetchPlans`)؛ اگه سرور در دسترس نبود از نسخه آفلاین (`FALLBACK_PLANS`) فقط برای **نمایش** استفاده می‌کنه — خرید حتماً اینترنت می‌خواد
- کد تخفیف: با `fetchQuote` سمت سرور چک می‌شه؛ دلیل ردشدن به فارسی توضیح داده می‌شه (`explainReason`)
- دکمه پرداخت: `checkout` → اگه ۱۰۰٪ تخفیف بود همون‌جا فعال می‌شه، وگرنه می‌ره به صفحه‌ی درگاه (`paymentUrl`)
- هشدار قرمز دستکاری ساعت اگه `tampered` باشه
- ⚠️ پرچم تستی: `TEST_GRANT_BUTTON` (تمدید بدون پرداخت، فقط محلی) — الان از قبل `false` است؛ اگه روشنش کردی قبل از انتشار برگردون

### `src/routes/pay.result.tsx` — نتیجه پرداخت
بعد از برگشت از درگاه اینجا فرود میای (`?paymentId=…&status=…`). وضعیت واقعی رو از سرور می‌پرسه و اگه هنوز نامشخصه هر ۲.۵ ثانیه دوباره می‌پرسه (حداکثر ~۱ دقیقه). اگه «paid» بود، اشتراک محلی آپدیت می‌شه و گِیت باز می‌شه. ۴ حالت نمایش: موفق 🎉 / لغوشده / ناموفق / هنوز نرسیده.

### قوانین و مقررات / حریم خصوصی / تماس — داخل تنظیمات
برای **اینماد** لازم بود. کلِّ متن (قوانین و مقررات + حریم خصوصی + تماس با ما) در یک **بخش بازشوی واحد** داخل صفحه‌ی تنظیمات (`routes/settings.tsx`) نمایش داده می‌شود — کامپوننت [`src/components/LegalContent.tsx`](../src/components/LegalContent.tsx). عمداً صفحه‌ی جدا یا لینکِ صفحه‌ی اول **ندارد** (به‌خواست مالک؛ روش تأیید اینمادش فرق داشت). اطلاعات تماس/هویتی از [`src/lib/legal-info.ts`](../src/lib/legal-info.ts) می‌آید — **فقط همان ۴ مقدار (نام مالک، تلفن، ایمیل، نشانی) را باید پر کنی**. دوزبانه؛ اگر کانتکست آماده نباشد فارسی پیش‌فرض است.

---

## ۸. قطعه‌های UI — پوشه `src/components/`

### `src/components/AppShell.tsx` — قاب اصلی
- **گِیت** (ترتیب: آنبوردینگ ← ورود ← اشتراک ← اپ) — تا وقتی دیتا لود نشده اسپلش (لوگو) نشون می‌ده
- ناوبری: آرایه `NAV` (۷ آیتم) — موبایل: نوار پایین (بدون تنظیمات)؛ دسکتاپ: سایدبار
- هدر بالا: لوگو + زنگوله اعلان‌ها (با عدد نخوانده‌ها) + آیکون تنظیمات
- مرکز اعلان‌ها (مودال) + دکمه «خواندن همه»
- پاپ‌آپ نظرسنجی ⭐: از جلسه ۳ به بعد، **حداکثر روزی یک‌بار** (ثابت `FEEDBACK_INTERVAL`) و حداکثر یک‌بار در هر بار باز کردن اپ، ۴ ثانیه بعد از بالا آمدن. اگه نمره ≤۳ بدی، می‌پرسه کدوم بخش ضعیفه
  - خودِ فرم در [`FeedbackModal.tsx`](../src/components/FeedbackModal.tsx) است — همان فرمی که دکمه‌ی «نظر بده» در تنظیمات هم باز می‌کند. یک کامپوننت، دو مصرف‌کننده؛ دو کپی بالاخره از هم عقب می‌افتاد.
  - قبلاً شرط «هر ۵ جلسه» بود، ولی هر بار **بالا آمدن** اپ یک جلسه است و روی وب هر رفرش هم یک بالا آمدن — پس یک روز عادی کار با برنامه چند بار پاپ‌آپ می‌داد. معیار الان زمان است (`meta.lastFeedbackAt`)، نه شمارش جلسه. فیلد قدیمی `meta.lastFeedbackSession` حذف شد.
  - ⚠️ شرطِ نمایش به شکل یک boolean حساب می‌شود و **همان** وابستگیِ افکت است، نه خودِ `db`. اگر `db` وابستگی باشد، هر تغییر حالت داخل آن ۴ ثانیه (تیک‌زدن یک عادت، زمان‌بندِ یادآور، ذخیره‌سازی) cleanup را اجرا می‌کند، تایمر را می‌کُشد، و اجرای بعدی به‌خاطر پرچمِ یک‌بار-در-هر-اجرا زود برمی‌گردد — یعنی پاپ‌آپ دیگر تا آخر آن نشست باز نمی‌شود.

### `src/components/habits.tsx` — همه‌چیزِ عادت‌ها
| بخش | توضیح |
|---|---|
| `HabitRow` | ردیف عادت در صفحه امروز: دایره تیک، کشیدن به راست = انجام شد / چپ = لغو (آستانه ۷۶ پیکسل)، دکمه‌های +/− برای عادت مقداری، ۷ نقطه‌ی هفته اخیر (`WeekChecks`)، نوار درصد، پیام «🔥 بیشتر از هدف!». کلیک روی اسم = مودال جزئیات (مقدار واقعی/حس/یادداشت). روزهای آینده فقط قابل مشاهده‌اند |
| `applyLog` | ثبت لاگ + تشخیص عبور از ۷۰٪/۱۰۰٪ هدف ماهانه (کلید جشن در `meta.celebrated` ذخیره می‌شه که تکراری نشه) |
| `useCelebration` + `CelebrationModal` | نمایش پاپ‌آپ جشن 🎉/🏆 |
| `HabitFormModal` | فرم ساخت/ویرایش عادت: اسم، دسته، نوع سنجش (شد‌/نشد یا مقداری)، واحد (تعدادی/زمانی)، روزهای هفته (حداقل ۱ روز)، هدف ماهانه، یادآور روزانه |
| `emptyDraft` | پیش‌فرض‌های فرم عادت جدید: همه روزهای هفته + **هدف ماهانه ۳۰** |
| `draftToHabit` / `habitToDraft` | تبدیل فرم ↔ عادت (۷ روز انتخاب‌شده = خودکار «روزانه») |
| `MonthCalendarGrid` | شبکه تقویمی ماه (استفاده در آنالیز): روز انجام‌شده = پر با رنگ دسته |

### بقیه کامپوننت‌ها

| آدرس | نقش |
|---|---|
| `src/components/tasks.tsx` | `TaskRow` (ردیف کار با همون حرکت سوایپ)، `TodayTodosCard` (کارت جمع‌شونده «کارهای امروز» در صفحه اصلی)، `addQuickTask` (افزودن سریع) |
| `src/components/WeekStrip.tsx` | نوار ۷ روز هفته: فلش/سوایپ برای هفته قبل/بعد، حلقه درصد دور هر روز، بج تعداد، حالت ایموجی (ژورنال) |
| `src/components/pwa.tsx` | `InstallBanner` (بنر «روتینو رو نصب کن» + راهنمای دو مرحله‌ای iOS) و `UpdateWatcher` (توست «نسخه جدید آماده‌ست» با دکمه بروزرسانی — هیچ‌وقت خودکار آپدیت نمی‌کنه) |
| `src/components/FeedbackModal.tsx` | فرم «نظرت درباره روتینو؟»: ۵ ستاره + (اگر ≤۳) انتخاب بخش ضعیف + توضیح. دو جا استفاده می‌شود: پاپ‌آپ خودکار `AppShell` و دکمه‌ی «نظر بده» در تنظیمات. `onDone` در هر دو حالتِ ثبت و «بعداً» صدا زده می‌شود، چون فراخوان باید در هر دو حالت ساعتِ آخرین پرسش را جلو ببرد |
| `src/components/LegalContent.tsx` | متن کامل «قوانین و مقررات + حریم خصوصی + تماس با ما» — داخل بخش بازشوی تنظیمات رندر می‌شود (لازمِ اینماد). اطلاعات تماس از `lib/legal-info.ts` |
| `src/components/ui.tsx` | 🧰 جعبه‌ابزار: `Button` `Input` `Card` `Modal` `Chip` `Progress` `SectionTitle` `EmptyState` `Logo` `CatIcon` + **`CATEGORY_ICONS`** (لیست آیکون‌های مجاز دسته‌ها — آیکون جدید رو اینجا اضافه کن) + انتخاب‌گرها (`TimePicker24`، `DatePickerCalendar`، `DurationPicker`، `DateTimePicker`) + `MiniBars` (نمودار میله‌ای همه صفحه‌ها) + `ColorWheel` (چرخ رنگ) + `formatDuration` (نمایش «۱ ساعت و ۳۰ دقیقه») |
| ~~`src/components/ui/` (پوشه)~~ | **حذف شد (تیر ۱۴۰۵)** — ۴۶ کامپوننت آماده shadcn که هیچ‌جا استفاده نمی‌شدن، همراه با ۳۸ پکیج npm بلااستفاده (radix/recharts/...) پاک شدن. UI واقعی اپ همیشه `ui.tsx` بوده. اگر روزی کامپوننت shadcn خواستی با `npx shadcn add` دوباره اضافه می‌شه |

---

## ۸.۵ صفحه‌ی معرفی (routino.me) — پوشه `landing/`

این بخش **جزء اپ ری‌اکتی نیست**؛ دو فایل HTML ایستا با CSS درون‌خطی که
`scripts/build-landing.mjs` آن‌ها را به ریشه‌ی `dist/` می‌سازد. اپ در `dist/app/`
می‌نشیند.

| فایل | نقش |
|---|---|
| `landing/index.template.html` | صفحه‌ی اول: نوار بالا (sticky نیست)، سرصفحه‌ی دوستونه با گوشی، نوارِ **۵**تاییِ نشان‌ها، **۴** ردیفِ یک‌درمیانِ قابلیت، ریلِ کارت‌های «چرا روتینو»، دکمه‌های گرفتن برنامه، بنرِ پایانی (بدون عکس)، فوتر |
| `landing/legal.template.html` | صفحه‌ی `/legal/` — متن از `src/lib/legal-text.json` می‌آید (همان منبعی که تنظیماتِ اپ هم می‌خواند) |
| `landing/shots/*.webp` | عکس‌های واقعی اپ؛ با `npm run shots` گرفته می‌شوند. برای گرفتن فقط یکی: `node scripts/shoot-landing.mjs timer-dark` |
| `landing/shots/clip-*-still.webp` | فریمِ اولِ کلیپ‌ها — **همین‌ها روی صفحه‌اند.** کلیپ‌های متحرکِ کنارشان (`clip-*.webp`، خروجیِ `npm run film`) دیگر ارجاع نمی‌شوند و منتشر نمی‌شوند |
| `scripts/lib/app-seed.mjs` | داده‌ی نمایشی و حالتِ «وارد شده»، مشترکِ عکس‌گیر و فیلم‌بردار |

**خروجی‌های بیلد که در قالب نیستند و `build-landing.mjs` می‌سازدشان:**

| فایل | چرا |
|---|---|
| `dist/og.jpg` | پیش‌نمایشِ اشتراک‌گذاری، ۱۲۰۰×۶۳۰، از `desktop-dark.webp` با sharp. jpg است نه webp چون بعضی پیش‌نمایش‌سازها webp را رد می‌کنند |
| `dist/robots.txt` | ⚠️ `public/robots.txt` هست ولی `vite build` می‌بردش به `dist/app/`، و خزنده فقط `/robots.txt` را می‌خواند — یعنی تا قبل از این عملاً robots نداشتیم. `/app/` عمداً `Disallow` است |
| `dist/sitemap.xml` | دو آدرس: `/` و `/legal/` |

⚠️ `desktop-dark.webp` دیگر در HTML ارجاع نمی‌شود (تگ og حالا به `og.jpg` اشاره
می‌کند)، پس `copyShots` آن را منتشر نمی‌کند — ولی `makeOgImage` از فایلِ **منبع**
در `landing/shots/` می‌خواندش. اگر آن را پاک کنی، بیلد می‌ایستد.

**⚠️ تصمیم مالک (مرداد ۱۴۰۵): صفحه‌ی اول هیچ حرفی از قیمت، اشتراک، خرید یا حتی
دوره‌ی آزمایشی نمی‌زند و فرم ثبت‌نام هم ندارد.** کارش فقط این است که بازدیدکننده
را با دکمه ببرد به `/app/`؛ حساب‌باز‌کردن و ۷ روز آزمایشی داخل خود اپ اتفاق
می‌افتد و صفحه‌ی خرید تنها وقتی دیده می‌شود که آن ۷ روز تمام شده باشد. اگر دنبال
کارت قیمت یا فرم شماره گشتی و پیدا نکردی: **عمداً حذف شده، از قلم نیفتاده.**

با همین تصمیم، ماژول `src/lib/signup-intent.ts` هم حذف شد. کارش این بود که
شماره و پلن را از آدرس (`/app/?start=…&plan=…`) بردارد و به `auth.tsx` و
`subscribe.tsx` برساند؛ حالا هیچ‌چیز آن پارامترها را تولید نمی‌کند.

**نکته‌های فنی که اگر ندانی خرابشان می‌کنی:**
- **صفحه فقط تاریک است و این انتخاب است، نه فراموشی.** `color-scheme: dark` هم
  ست شده تا اسکرول‌بار و کنترل‌های پیش‌فرض مرورگر با صفحه یکی باشند.
- **پالت عیناً توکن‌های `.dark` خودِ اپ است** (`src/styles.css`)، فقط از oklch به
  hex برگردانده تا این فایل به هیچ ابزاری وابسته نباشد:
  `--ink`=background `#0e0a08`، `--card`=card `#1a1512`، `--edge`=border
  `#322d29`، `--brand`=primary `#fe7825`، `--fg`=foreground، `--fg-2`=muted.
  فونت هم همان `"Vazirmatn", system-ui, sans-serif`ِ اپ است. قبلاً پالتِ جدا و
  روشن‌تری داشت و بازدیدکننده موقع ورود به اپ رنگ زیرِ پایش عوض می‌شد.
  **اگر `.dark` در استایلِ اپ عوض شد، این‌ها را هم دستی بروز کن** — پیوندِ
  خودکاری بینشان نیست. `--brand-rgb` هم هست چون درخشش‌ها alpha می‌خواهند و
  `rgba()` رنگِ hex قبول نمی‌کند؛ با عوض‌شدنِ برند این را هم عوض کن.
- **یک استثنا در پالت:** پس‌زمینه‌ی `.scene` عمداً `--card` است نه `--ink-2`.
  با پالتِ تیره‌ی اپ، `--ink-2` تقریباً همان پس‌زمینه‌ی صفحه است و صحنه‌ی پشتِ
  گوشی کاملاً ناپدید می‌شد.
- **آمار و نظرِ کاربر جعل نمی‌کنیم.** جای «۱۵ میلیون دانلود» و «امتیاز اپ‌استور»
  را نوارِ چهارتاییِ `.awards` گرفته و جای دیوارِ نظرِ کاربران را ریلِ کارت‌های
  `#why`؛ هر دو فقط حرف‌های راستِ محصول‌اند. تا وقتی عددِ واقعی نداری اینجا عدد
  نگذار و تا کاربرِ واقعی نداری نقلِ‌قول نگذار.
- **چیدمان (مرداد ۱۴۰۵) عمداً عیناً از productiveapp.io گرفته شده؛ مالک همین را
  خواست.** ولی فقط «شکل» گرفته شده: رنگ، متن، فونت، آیکون، تصویر و هر ادعایی
  مالِ خودمان است. زمینه هم عمداً مشکیِ خنثی نیست، قهوه‌ایِ خیلی تیره است تا
  نارنجیِ برند رویش بنشیند. سه چیز که **عمداً حذف شدند** چون در Productive
  نیستند و اگر جایی ندیدی‌شان از قلم نیفتاده‌اند: دکمه‌ی شناورِ چسبانِ پایینِ
  موبایل (`.dock`)، انیمیشنِ `rise` روی تیتر و زیرنویسِ بخش‌ها، و
  sticky/blur/border-on-scroll روی نوار بالا. بخشِ جداگانه‌ی لپ‌تاپ هم حذف شد.
- **قاعده‌ی انیمیشن یک خط است: هیچ متنی حرکت نمی‌کند.** تنها انیمیشنِ اسکرولی،
  ظاهرشدنِ تصویرِ هر ردیفِ قابلیت است با **opacity خالص** (`.fade` → `.fade.lit`،
  نیم‌ثانیه، بدون هیچ `translate`)، یک‌بار، با `IntersectionObserver` و بعد
  `unobserve`. آستانه عمداً `0.5` است نه `1`: با ۱، روی گوشیِ خوابیده عکس از خودِ
  صفحه بلندتر می‌شود و شرط هیچ‌وقت برقرار نمی‌شد — یعنی عکس تا ابد نامرئی.
- **ریلِ «چرا روتینو» اسکرول نمی‌شود.** یک ردیفِ وسط‌چینِ ثابت است که از دو طرف
  بیرون می‌زند و کلیپ می‌شود؛ چرخش با جابه‌جا کردنِ گره در DOM انجام می‌شود
  (`appendChild(first)` = بعدی، `insertBefore(last, first)` = قبلی)، نه با
  `scrollLeft`/`scrollIntoView` — چون در صفحه‌ی راست‌چین هر موتورِ مرورگر
  `scrollLeft` را جور دیگری می‌شمارد. خودکار هر ۵ ثانیه، و هر کلیک روی
  قبلی/بعدی تایمر را از نو کوک می‌کند. فقط کارتِ وسط `opacity: 1` است.
  محوشدنِ لبه‌ها با `mask-image` است نه لایه‌ی رنگی، چون پس‌زمینه‌ی این بخش
  یکدست نیست؛ و `--fade` عمداً پیکسل است نه درصد، وگرنه روی موبایل محوشدگی
  می‌افتاد روی خودِ کارتِ وسط.
- **در حالتِ ستونیِ موبایل، `flex: none` روی ستون‌ها لازم است نه اضافه.** هر
  `flex-basis`ی که برای عرض گذاشته شده، وقتی `flex-direction` ستونی می‌شود به
  **ارتفاع** تبدیل می‌گردد و زیرِ محتوا فضای خالیِ بی‌دلیل باز می‌کند. یک بار
  همین روی `.home-copy` ۲۰۰ پیکسل حفره زیرِ دکمه‌ی سرصفحه ساخت.
- **عرضِ گوشیِ داخلِ `.scene` درصدی است (`70%`)، نه عددِ ثابت.** با عددِ ثابت،
  روی لپ‌تاپ صحنه پهن می‌شد و گوشی همان‌قدر می‌ماند؛ نتیجه یک جعبه‌ی قهوه‌ایِ
  خالی بود با یک گوشیِ کوچک وسطش.
- **لینک دانلود اندروید از متغیر `ANDROID_DOWNLOAD_URL` در زمان build می‌آید.**
  تا وقتی APK در فضای ابری آپلود نشده، `build-landing.mjs` دکمهٔ غیرفعال
  «بعد از انتشار» می‌سازد. آدرس فقط وقتی پذیرفته می‌شود که HTTPS معتبر باشد؛
  پس دیگر برای فعال‌کردن دانلود نباید HTML قالب را دستی تغییر داد.
- **هیچ متنی برای دیده‌شدن به انیمیشن وابسته نیست.** حالتِ پیش‌فرضِ هر عنصر
  «دیده می‌شود» است؛ کلاسِ `.js` (که اسکریپت کوچکِ داخل `head` می‌گذارد) آن را
  مخفی می‌کند تا انیمیشن اجرا شود. اگر جاوااسکریپت خطا بدهد یا اجرا نشود،
  بازدیدکننده صفحه‌ی کامل را می‌بیند نه صفحه‌ی سفید. این `.js` را برندار.
- **هیچ فیلمی پخش نمی‌شود؛ فقط عکسِ ثابت با انیمیشنِ ظاهرشدن (تصمیم مالک، مرداد
  ۱۴۰۵).** تصویرها همان عکسِ واقعیِ برنامه‌اند (`clip-*-still.webp`، فریمِ اولِ
  کلیپ‌ها). کلیپ‌های متحرک و مکانیزمِ `.reel` که آن‌ها را وسطِ اسکرول جایگزین
  می‌کرد **حذف شده‌اند**. اثرش: تصویرهای منتشرشده از ۸۱۱ به ۱۹۹ کیلوبایت رسید.
  فایل‌های `clip-*.webp` هنوز در `landing/shots/` هستند ولی چون ارجاع نمی‌شوند،
  `copyShots` آن‌ها را به `dist/` نمی‌برد. اگر روزی خواستی برشان گردانی،
  `npm run film` و `scripts/film-landing.mjs` سرِ جایشان‌اند.
- **گوشیِ سرصفحه پنج عکسِ روی‌هم است که یک بار پشتِ سرِ هم ظاهر می‌شوند** (هرکدام
  ۰٫۵ ثانیه، با تأخیرِ ۱ / ۱٫۵ / ۲ / ۲٫۵ ثانیه و `fill: both`)، و آخری روی همه
  می‌ماند. پس **ترتیبِ `<img>`ها معنا دارد**: «امروز» عمداً آخر است، چون بعد از
  سه ثانیه همان روی گوشی می‌ماند. انیمیشن پشتِ کلاسِ `.js` است، پس بدون
  جاوااسکریپت فقط عکسِ اول می‌ماند و گوشی خالی نمی‌شود.
- **عکسِ سرصفحه با `fetchpriority=high` و بدون lazy است** (همان LCP)، و
  `<link rel=preload>` سرِ صفحه هم به همان اشاره می‌کند — یعنی به اولین عکسِ
  اسلایدر، نه به «امروز» که آخری‌ست.
- **گوشیِ داخلِ `.scene` عمداً از پایین بریده می‌شود.** صحنه `overflow: hidden`
  دارد و گوشی با `align-items: start` از بالا فاصله می‌گیرد؛ اگر وسط‌چین بگذاری،
  چون گوشی از خودِ صحنه بلندتر است لبه‌ی گردِ **بالایش** هم بریده می‌شود و
  زشت می‌شود.
- **در ستونی‌شدنِ ردیف‌ها (زیر ۹۰۰ پیکسل) `.feat-art` باید `width: 100%` بگیرد.**
  `.feat` مقدارِ `align-items: center` دارد، پس بچه‌ها کش نمی‌آیند و ستونِ تصویر
  به عرضِ ذاتیِ خودش (۵۲۰ پیکسل) می‌رسد و از صفحه می‌زند بیرون. چون
  `body { overflow-x: hidden }` است، این سرریز اسکرولِ افقی نمی‌سازد و
  **بی‌صدا محتوا را می‌بُرد**.
- **ریلِ کارت‌های `#why` با `scrollIntoView` جابه‌جا می‌شود، نه با `scrollLeft`.**
  در صفحه‌ی راست‌چین هر موتور عددِ `scrollLeft` را جور دیگری می‌شمارد (کروم از صفر
  به منفی می‌رود) و دکمه‌ها روی یکی کار می‌کنند و روی بقیه تکان نمی‌خورند.
  `block: "nearest"` هم لازم است، وگرنه مرورگر کلِ صفحه را عمودی می‌پراند.
- **برای `prefers-reduced-motion` باید `behavior: "instant"` بدهی، نه `"auto"`.**
  `"auto"` یعنی «هرچه CSS گفته» و CSS روی `.rail` مقدارِ `scroll-behavior: smooth`
  دارد؛ یعنی با `"auto"` کاربری که انیمیشن را خاموش کرده باز هم حرکتِ نرم
  می‌گرفت.
- `copyShots` فقط عکس‌هایی را منتشر می‌کند که HTML خروجی واقعاً به آن‌ها ارجاع
  داده؛ یک `landing/shots/*.webp` یتیم با سایت منتشر نمی‌شود.
- لوگو همان `public/favicon.svg` خودِ اپ است (`copyLogo` آن را به ریشه‌ی سایت
  کپی می‌کند)، نه یک تیکِ بازسازی‌شده با CSS — تا سایت و اپ هیچ‌وقت دو نشان
  متفاوت نشان ندهند.
- نوار چسبانِ پایین (`.dock`) فقط روی موبایل و بعد از ۷۰٪ ارتفاع صفحه ظاهر
  می‌شود.

---

## ۹. استایل — `src/styles.css`

- متغیرهای رنگ در `:root` (تم روشن) و `.dark` (تم تیره): `--primary` (نارنجی برند)، `--background`، `--card`، `--success`، `--destructive` و...
- رنگ برند دلخواه کاربر این متغیرها رو **در لحظه** بازنویسی می‌کنه (از `state/app.tsx`)
- کلاس‌های کمکی: `card-surface` (ظاهر کارت‌ها)، `page-stagger` (انیمیشن ورود صفحه)، `animate-check-pop` و `animate-row-flash` (انیمیشن تیک)، `pb-nav`/`pt-safe`/`py-screen-safe` (فاصله‌های امن موبایل)
- فونت: وزیرمتن (`@fontsource/vazirmatn`)

---

## ۱۰. تست‌ها و فایل‌های تنظیمات فرانت

| آدرس | نقش |
|---|---|
| `src/lib/*.test.ts` و `src/lib/db/*.test.ts` | تست‌های خودکار (backup، chart، phone، diff، migrate، persist) — با `npm test` اجرا می‌شن |
| `src/test/setup.ts` | آماده‌سازی محیط تست (IndexedDB مصنوعی) |
| `vite.config.ts` | تنظیمات بیلد: خروجی وب=`dist/` موبایل=`www/`؛ تنظیمات PWA (سرویس‌ورکر روی موبایل **خاموش** — عمدیه)؛ پروکسی `/v1` به `localhost:3000` در توسعه |
| `vitest.config.ts` | تنظیمات اجرای تست |
| `capacitor.config.ts` | شناسه اپ (`com.routino.app`)، اسم اپ، پوشه وب (`www`)، تنظیمات اسپلش و نوتیف |
| `tsconfig.json` | تنظیمات TypeScript (مسیر میانبر `@/` = `src/`) |
| `eslint.config.js` / `.prettierrc` | قوانین کیفیت و فرمت کد |
| `package.json` | دستورها: `dev`، `build`، `build:mobile`، `cap:android`، `test`، `icons` |
| `public/` | آیکون‌ها + `manifest.webmanifest` (مشخصات PWA) |
| `assets/` + `scripts/generate-icons.mjs` | تصویر خام لوگو + اسکریپت ساخت همه سایزهای آیکون (`npm run icons`) |
| `android/` و `ios/` | پروژه‌های نیتیو (خروجی Capacitor). فقط برای: مجوزها، آیکون نیتیو، امضای انتشار. دیپ‌لینک `routino://` در `android/app/src/main/AndroidManifest.xml` ثبت شده |
| `www/` و `dist/` | ⛔ خروجی بیلد — دستی ویرایش نکن |

---

## ۱۱. الگوهایی که همه‌جای فرانت تکرار می‌شن (یاد بگیری، همه‌جا رو می‌فهمی)

1. **دوزبانه:** هر متن به شکل `t("متن فارسی", "English text")` — برای عوض‌کردن متن، همون صفحه رو باز کن و متن فارسی رو جستجو کن.
2. **تغییر دیتا:** همیشه `update((d) => ({ ...d, چیزِ‌جدید }))` — هیچ‌وقت مستقیم چیزی تغییر نمی‌کنه؛ نسخه‌ی جدید ساخته می‌شه (این چیزیه که ذخیره‌ی خودکار بهش وابسته‌ست).
3. **کلید تاریخ:** روزها همیشه رشته `"2026-07-18"` (میلادی) هستن حتی وقتی تقویم شمسیه — شمسی فقط موقع «نمایش» ساخته می‌شه.
4. **عادت زمانی:** هدف و مقدار همیشه به **دقیقه** ذخیره می‌شن؛ «۱ ساعت و نیم» فقط شکل نمایششه (`formatDuration`).
5. **آینده ممنوع:** روزهای آینده فقط قابل مشاهده‌ان (`isFuture` در HabitRow، `disableFuture` در WeekStrip).
